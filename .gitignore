# Large binary artifacts. Build them via scripts/ or download from the GitHub
# release. GitHub blocks files over 100 MB in git; the image alone is 8 GB.
artifacts/*.img
artifacts/*.img.*
artifacts/*.pkg.tar.zst
artifacts/steamdeck-oobe-repair-*

# Local keys
artifacts/trashcan_key.pub
artifacts/trashcan_key

# macOS and Electron noise
.DS_Store
._*
DawnCache/
GPUCache/
